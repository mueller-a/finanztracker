# Project Context

The UI follows the central design system. All visual decisions
must follow the rules in [`.claude/skills/design-system/SKILL.md`](../.claude/skills/design-system/SKILL.md).

Never use inline styles or arbitrary color values.
Use MUI theme tokens (e.g. `accent.positiveSurface`) instead of hex codes.
